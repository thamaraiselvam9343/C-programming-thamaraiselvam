#include <stdio.h>
int main() {
    int N;
    long long reverse = 0;
    scanf("%d", &N);
    while (N > 0) {
        int digit = N % 10;     
        reverse = reverse * 10 + digit;
        N = N / 10;           
    }
    printf("%lld", reverse);
}
