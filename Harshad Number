#include <stdio.h>
int main() {
    int N, temp, sum = 0;
    scanf("%d", &N);
    temp = N;
    while(temp > 0) {
        sum += temp % 10;
        temp /= 10;
    }
    if(N % sum == 0)
        printf("Harshad Number");
    else
        printf("Not a Harshad Number");
}
