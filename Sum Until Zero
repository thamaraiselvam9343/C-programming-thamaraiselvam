#include <stdio.h>
int main() {
    long long num, sum = 0;
    while (1) {
        scanf("%lld", &num);
        if (num == 0)
            break;
        sum += num;
    }
    printf("Total Sum = %lld", sum);
}
